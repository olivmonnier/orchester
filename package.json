{
  "name": "orchester",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "babelify": "browserify src/index.js -o dist/bundle.js -t [ babelify --presets [ es2015 ] ]",
    "uglify": "uglifyjs dist/bundle.js -o dist/bundle.min.js -c",
    "build": "npm run babelify && npm run uglify",
    "dev": "npm run build && static-server -p 3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.24.0",
    "babelify": "^7.3.0",
    "browserify": "^14.1.0",
    "static-server": "^2.0.4",
    "uglify-js": "^2.8.14"
  }
}
